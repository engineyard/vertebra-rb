#!/usr/bin/env ruby

require 'rubygems'
require 'irb'
require 'drb'
require File.dirname(__FILE__) + '/../lib/vertebra'

@agent = DRbObject.new(nil, "druby://localhost:#{ARGV[0] || 10392}")
ARGV[0] = nil if ARGV[0]

IRB.start

